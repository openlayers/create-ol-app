{
  "name": "create-ol-app",
  "version": "1.0.0",
  "description": "Initialize a new OpenLayers application",
  "main": "src/main.js",
  "scripts": {
    "lint": "eslint src test",
    "pretest": "npm run lint",
    "test": "mocha --recursive test"
  },
  "bin": {
    "create-ol-app": "src/main.js"
  },
  "files": [
    "src/"
  ],
  "keywords": [
    "openlayers",
    "ol",
    "map",
    "mapping"
  ],
  "license": "BSD-2-Clause",
  "devDependencies": {
    "eslint": "^7.29.0",
    "eslint-config-openlayers": "^15.1.0",
    "mocha": "^9.0.1"
  },
  "eslintConfig": {
    "extends": "openlayers",
    "parserOptions": {
      "ecmaVersion": 2017
    }
  },
  "dependencies": {
    "commander": "^7.2.0",
    "degit": "^2.8.4",
    "fs-extra": "^10.0.0",
    "validate-npm-package-name": "^3.0.0"
  }
}
